# 按钮绑定审计 — 任务总结

**时间**: 2026-06-24 09:24
**目标文件**: `D:\GitHub-tongbuwenjianjia\CRMsystem\CRMsystem\index.html` (687KB)

## 审计目标
分析 DOMContentLoaded 按钮绑定、客户池/主列表/渠道/设置/存量管理各页面的按钮 HTML id 与 JS 绑定对应关系。

## 方法
通过 `findstr` 搜索所有 `getElementById`、`.onclick`、`data-act` 出现位置，交叉验证 HTML 标记中的 `id="..."` 是否存在。

## 主要发现
1. **全部 DOMContentLoaded 直绑按钮 (50+) 均匹配** — 无断链
2. **`pool-btn-export` 不存在** — 客户池无独立导出按钮
3. **`btn-export-all` 不存在** — 主列表无"一键导出全部"按钮
4. **`btn-add-tag/level/source` 实际为 `btn-new-tag/level/source`** — 命名约定不一致
5. **`save-levels/save-sources` 不存在** — 设计上弹窗即时保存
6. **`btn-sq-setup` / `btn-sq-cancel` 有重复绑定** — 代码冗余但功能正常
7. **data-act 绑定** — 5个渲染函数 × 4种动作全部正确闭包绑定

## 交付物
输出完整审计报告到 `button-binding-audit.md`，包含逐项匹配表、问题标注和设计模式分析。
