# CRM 系统浮层/弹窗数据列与关联正确性审计报告

**审计时间**: 2026-06-23 20:33 GMT+8  
**审计文件**: `D:\GitHub-tongbuwenjianjia\CRMsystem\CRMsystem\index.html`  
**审计范围**: 全部 8 个浮层/弹窗/表单的字段完整性、关联正确性

---

## 数据模型概览

`Store.data` 存储以下客户数组：
| 数据键 | 说明 | 典型字段 |
|--------|------|----------|
| `customers` | 主客户列表 | 全部 CRM 字段 |
| `poolCustomers` | 客户池 | 子集（无 company/level/tags/platforms/channels/amount） |
| `stockCustomers` | 存量客户列表 | accountName/cid/consultant/accountOpenDate 等 |
| `brandProducts` | 品牌产品管理 | 同 stockCustomers |
| `serviceFees` | 服务费管理 | 同 stockCustomers |
| `qiFuProducts` | 企服产品管理 | 同 stockCustomers |

---

## 1. 客户详情弹窗 `openCustomerDetail`（约行 6901）

### 显示字段

| 序号 | 显示标签 | 数据字段 | 数据模型一致性 |
|------|---------|---------|---------------|
| 1 | 手机号 | `c.phone` | ✅ 一致 |
| 2 | 公司名称 | `c.company` | ✅ 一致 |
| 3 | 微信 | `c.wechat` | ✅ 一致 |
| 4 | 邮箱 | `c.email` | ✅ 一致 |
| 5 | 性别 / 年龄 | `c.gender` + `c.age` | ✅ 一致（合并显示） |
| 6 | 客户等级 | `c.level` | ✅ 一致 |
| 7 | 客户来源 | `c.source` | ✅ 一致 |
| 8 | 成交状态 | `c.status` | ✅ 一致 |
| 9 | 意向产品 | `c.product` | ✅ 一致 |
| 10 | 预算范围 | `c.budget` | ✅ 一致 |
| 11 | 下次跟进日期 | `c.nextFollowDate` | ✅ 一致 |
| 12 | 录入时间 | `c.createdAt` | ✅ 一致 |
| 13 | 联系地址 | `province+city+district+address` | ✅ 一致 |
| 14 | 一级行业 | `c.industryLevel1` | ✅ 一致 |
| 15 | 二级行业 | `c.industryLevel2` | ✅ 一致 |
| 16 | 客户网址 | `c.website` | ✅ 一致 |
| 17 | 客户在投平台 | `c.platforms` | ✅ 一致 |
| 18 | 客户标签 | `c.tags` | ✅ 一致 |
| 19 | 备注 | `c.remark` | ✅ 一致 |
| 20 | 跟进记录时间线 | `c.remarkHistory` | ✅ 一致（含图片） |

**字段完整性：** 与 `openCustomerForm` 表单字段完全对齐，无缺少、无多余 ⭐

### 按钮正确性

| 按钮 ID | 行为 | 正确性 |
|---------|------|--------|
| `#editDetail` | **跨表匹配逻辑**：先检查 brandProducts → qiFuProducts → serviceFees → stockCustomers → 最后 openCustomerForm | ⚠️ **潜在Bug**：检查 `brandProducts` 等存量表使用 `.find(x => x.id === c.id)` → 若主客户列表中某个客户的 id 恰好与存量客户 id 相同（uuidv4 冲突概率极低但逻辑可疑），会错误路由到存量表单 |
| `#exportOne` | 调用 `buildExcelData([c], true)` 导出单客户 26 列 | ✅ 正确 |
| `#delDetail` | 调用 `promptPassword` 二次验证后删除 | ✅ 安全设计 |

---

## 2. 客户池详情弹窗 `openPoolCustomerDetail`（约行 5985）

### 显示字段

| 序号 | 显示标签 | 数据字段 | 数据模型一致性 |
|------|---------|---------|---------------|
| 1 | 手机号 | `c.phone` | ✅ 一致 |
| 2 | **公司名称** | `c.company` | ❌ **池表单无 company 字段！** |
| 3 | 微信 | `c.wechat` | ✅ 一致 |
| 4 | 邮箱 | `c.email` | ✅ 一致 |
| 5 | 性别 / 年龄 | `c.gender` + `c.age` | ✅ 一致（合并显示） |
| 6 | 客户来源 | `c.source` | ✅ 一致 |
| 7 | 成交状态 | `c.status` | ✅ 一致 |
| 8 | 意向产品 | `c.product` | ✅ 一致 |
| 9 | 预算范围 | `c.budget` | ✅ 一致 |
| 10 | 下次跟进日期 | `c.nextFollowDate` | ✅ 一致 |
| 11 | 录入时间 | `c.createdAt` | ✅ 一致 |
| 12 | 联系地址 | `province+city+district+address` | ✅ 一致 |
| 13 | 一级行业 | `c.industryLevel1` | ✅ 一致 |
| 14 | 二级行业 | `c.industryLevel2` | ✅ 一致 |
| 15 | 客户网址 | `c.website` | ✅ 一致 |
| 16 | 备注 | `c.remark` | ✅ 一致 |
| 17 | 跟进记录时间线 | `c.remarkHistory` | ⚠️ **无图片支持**（无 imgHtml 代码） |

### 与主客户详情对比的差异（缺少的字段）

| 字段 | 主详情 | 池详情 | 风险 |
|------|--------|--------|------|
| 客户等级 `level` | ✅ 显示 | ❌ 不显示 | 低（池无等级字段） |
| 客户标签 `tags` | ✅ 显示 | ❌ 不显示 | 低（池表单无 tags） |
| 客户在投平台 `platforms` | ✅ 显示 | ❌ 不显示 | 低（池无 platforms 字段） |

### 按钮正确性

| 按钮 ID | 行为 | 正确性 |
|---------|------|--------|
| `#poolEditDetail` | 关闭弹窗 → `openPoolCustomerForm(c)` | ✅ 正确 |
| `#poolExportOne` | 调用 `buildExcelData([c], true)` | ⚠️ **导出格式不匹配！** 复用主客户 26 列导出，包含 company/level/channels/platforms 等池表单无法编辑的字段 |
| `#poolDelDetail` | 直接 `Store.data.poolCustomers.filter` → `Store.save()` | ⚠️ **无 promptPassword 二次验证**（主详情有） |

---

## 3. 存量客户详情弹窗 `openStockCustomerDetail`（约行 6516）

### 显示字段

| 序号 | 显示标签 | 数据字段 | 数据模型一致性 |
|------|---------|---------|---------------|
| 1 | 账户ID | `c.cid` | ✅ 一致 |
| 2 | 账户名称 | `c.accountName \|\| c.name` | ✅ 一致 |
| 3 | 联系人 | `c.name` | ✅ 一致 |
| 4 | 联系电话 | `c.phone` | ✅ 一致 |
| 5 | 微信号 | `c.wechat` | ✅ 一致 |
| 6 | 公司名称 | `c.company` | ✅ 一致 |
| 7 | 对应顾问 | `c.consultant` | ✅ 一致 |
| 8 | 一级行业 | `c.industryLevel1` | ✅ 一致 |
| 9 | 二级行业 | `c.industryLevel2` | ✅ 一致 |
| 10 | 网站URL | `c.website` | ✅ 一致 |
| 11 | 开户日期 | `c.accountOpenDate` | ✅ 一致 |
| 12 | 客户等级 | `getChannelField(c, module, 'level') \|\| c.level` | ✅ 一致（模块隔离） |
| 13 | 客户来源 | `c.source` | ✅ 一致 |
| 14 | 成交状态 | `getChannelField(c, module, 'status') \|\| c.status` | ✅ 一致（模块隔离） |
| 15 | 意向产品 | `c.product` | ✅ 一致 |
| 16 | 预算范围 | `c.budget` | ✅ 一致 |
| 17 | 预计金额(元) | `getChannelField(c, module, 'amount') \|\| c.amount` | ✅ 一致 |
| 18 | 下次跟进日期 | `getChannelField(c, module, 'nextFollowDate') \|\| c.nextFollowDate` | ✅ 一致 |
| 19 | 录入时间 | `c.createdAt` | ✅ 一致 |
| 20 | 联系地址 | `province+city+district+address` | ✅ 一致 |
| 21 | 客户在投平台 | `c.platforms` | ✅ 一致 |
| 22 | 客户标签 | `getChannelField(c, module, 'tags')` | ✅ 一致（模块隔离） |
| 23 | 备注 | `c.remark` | ✅ 一致 |
| 24 | 跟进记录时间线 | `getChannelField(c, module, 'remarkHistory')` | ✅ 一致（含图片） |

**字段完整性：** ✅ 与 `openStockCustomerForm` 完全对齐，通过 `channelData` 机制实现模块间字段隔离。

### 按钮正确性

| 按钮 ID | 行为 | 正确性 |
|---------|------|--------|
| `#editDetail` | `openStockCustomerForm(module, dataKey, c)` | ✅ 正确 |
| `#exportOne` | `buildStockExcelData([c], module)` | ✅ 正确（22列存量格式） |
| `#delDetail` | `promptPassword` 二次验证 → 删除 | ✅ 安全设计 |

---

## 4. 统计浮层 `stat-popup`（行 2391-2397 HTML / 7868 JS）

### HTML 结构

```html
<div class="stat-popup-overlay" id="stat-popup-overlay"></div>
<div class="stat-popup" id="stat-popup">
  <div class="stat-popup-header">
    <span class="stat-popup-title" id="stat-popup-title">客户列表</span>
    <button class="stat-popup-close" id="stat-popup-close">×</button>
  </div>
  <div class="stat-popup-body" id="stat-popup-body"></div>
</div>
```

### 打开逻辑 `showStatPopup(type)`（行 7868）

| 类型 | 条件筛选 | 数据来源 |
|------|---------|---------|
| `'total'` | 全部 `customers`（排除 deleted） | `Store.data.customers` ✅ |
| `'today'` | `createdAt >= todayStart.getTime()` | `Store.data.customers` ✅ |
| `'month'` | `createdAt >= monthStart.getTime()` | `Store.data.customers` ✅ |
| `'vip'` | `level` 在前 2 个等级中 | `Store.data.customers` ✅ |
| `'follow'` | `status === '跟进中'` 或 `nextFollowDate >= today` | `Store.data.customers` ✅ |

**数据来源正确性**: ✅ 从 `Store.data.customers` 直接读取，非 BI 模块数组

**触发方式**: 仪表盘 5 个 `stat-card` → `card.dataset.statType` → `showStatPopup(card.dataset.statType)` ✅

**点击客户跳转**: 点击 `stat-popup-item` → 保存 type → 关闭 stat-popup → `openCustomerDetail(customer, true)` → 从详情关闭后 `_restoreStatPopup()` 恢复 ✅

## 5. 客户浮层面板 `customer-float-panel`（行 2402 HTML / 8388 JS）

### HTML 结构

```html
<div class="customer-float-panel" id="customer-float-panel">
  <div class="customer-float-header">
    <span class="customer-float-name" id="customer-float-name"></span>
    <span class="customer-float-badges" id="customer-float-badges"></span>
    <button class="customer-float-close" id="customer-float-close">×</button>
  </div>
  <div class="customer-float-body" id="customer-float-body"></div>
  <div class="customer-float-footer" id="customer-float-footer"></div>
</div>
```

### 显示字段 (`showCustomerFloat`)

| 区域 | 字段 | 数据源 |
|------|------|--------|
| 标题 | 姓名 | `customer.name` |
| 徽章 | 等级 | `customer.level` + `levelColor()` |
| 徽章 | 状态 | `customer.status` |
| 徽章 | 预警标签 | `source` 参数（overdue/upcoming/agenda） |
| 徽章 | 客户标签 | `customer.tags` |
| 网格 | 手机号 | `customer.phone` |
| 网格 | 公司名称 | `customer.company` |
| 网格 | 微信 | `customer.wechat` |
| 网格 | 来源 | `customer.source` |
| 网格 | 渠道 | `getCustomerChannels(customer)` |
| 网格 | 意向产品 | `customer.product` |
| 网格 | 预算 | `customer.budget` |
| 网格 | 最后跟进日期 | `customer.remarkHistory[0].time` 或 `customer.createdAt` |
| 网格 | 下次跟进日期 | `customer.nextFollowDate` |
| 网格 | 流失预警 | 计算 `daysSinceLast > followupDays` |
| 网格 | 最近跟进记录 | `customer.remarkHistory[0].text` |
| 议程 | 下次沟通事项 | `buildAgendaContent()` |

**缺失字段（对比主详情）：** email, gender, age, address, industryLevel1/2, website, platforms, amount, remark ✅ 均为合理精简（浮层面板定位为快速预览）

### 按钮正确性

| 按钮 | 行为 | 正确性 |
|------|------|--------|
| `#float-close-btn` | `closeCustomerFloat()` | ✅ |
| `#float-detail-btn` | 关闭浮层 → 150ms 后 `openCustomerDetail(customer, false)` | ✅ |

**触发方式**: 表格中 `.cell-name-link` / `.cell-company-link` 点击委托 ✅

---

## 6. 新增/编辑客户表单 `openCustomerForm`（约行 4247）

### 字段完整性

| 区块 | 字段 | 类型 | 必填 |
|------|------|------|------|
| 基础信息 | name | text | ✅ |
| 基础信息 | company | text | ❌ |
| 基础信息 | gender | select(男/女/其他) | ❌ |
| 基础信息 | age | number | ❌ |
| 基础信息 | phone | text(11位) | ❌ |
| 基础信息 | wechat | text | ❌ |
| 基础信息 | email | email | ❌ |
| 基础信息 | source | select(动态来源) | ❌ |
| 基础信息 | channels | checkbox-group(6渠道) | ❌ |
| 业务信息 | product | text | ❌ |
| 业务信息 | budget | text | ❌ |
| 业务信息 | amount | number | ❌ |
| 业务信息 | status | select(4种状态) | ❌ |
| 业务信息 | nextFollowDate | date | ❌ |
| 业务信息 | website | text | ❌ |
| 业务信息 | industryLevel1 | select-industry(联动) | ❌ |
| 业务信息 | industryLevel2 | select-industry2(联动) | ❌ |
| 业务信息 | platforms | checkbox-group(8平台) | ❌ |
| 联系地址 | province | text | ❌ |
| 联系地址 | city | text | ❌ |
| 联系地址 | district | text | ❌ |
| 联系地址 | address | textarea | ❌ |
| 沟通备注 | remark | textarea(截图) | ❌ |
| 标签 | tags | tag-chip 多选 | ❌ |

**保存逻辑**: 收集所有 `[data-key]` 元素 + tags + channels + platforms → 校验 → escapeHTML(非remark) → 合并到客户对象 ✅

**校验规则**: name 必填、phone 11位数字、email 格式、age 0-120 ✅

---

## 7. 新增/编辑存量客户表单 `openStockCustomerForm`（约行 6641）

### 字段完整性

| 区块 | 字段 | 类型 | 备注 |
|------|------|------|------|
| 基础信息 | cid | text | 账户ID |
| 基础信息 | accountName | text | 账户名称 |
| 基础信息 | name | text | 联系人（必填） |
| 基础信息 | phone | text | 联系电话 |
| 基础信息 | wechat | text | 微信号 |
| 基础信息 | company | text | 公司名称 |
| 基础信息 | consultant | text | 对应顾问 |
| 业务信息 | industryLevel1 | select-industry | 联动 |
| 业务信息 | industryLevel2 | select-industry2 | 联动 |
| 业务信息 | website | text | 网站URL |
| 业务信息 | accountOpenDate | date | 开户日期 |
| 业务信息 | source | select | 客户来源 |
| 业务信息 | level | select | **模块隔离** (channelData) |
| 业务信息 | status | select | **模块隔离** (channelData) |
| 业务信息 | product | select-product | 动态产品标签 |
| 业务信息 | budget | text | |
| 业务信息 | amount | number | **模块隔离** (channelData) |
| 业务信息 | nextFollowDate | date | **模块隔离** (channelData) |
| 业务信息 | platforms | checkbox-group | 8平台 |
| 联系地址 | province | text | |
| 联系地址 | city | text | |
| 联系地址 | district | text | |
| 联系地址 | address | textarea | |
| 客户标签 | tags | checkbox | **模块隔离** (channelData) |
| 沟通备注 | remark | textarea | **模块隔离** (channelData) |

**保存逻辑**: ✅ 使用 `setChannelField` 隔离模块特定字段（level/status/amount/nextFollowDate/tags），remark 通过 `addChannelRemark` 保存。实体字段直接 `Object.assign(c, updates)`。

---

## 8. 新增/编辑客户池表单 `openPoolCustomerForm`（约行 5823）

### 字段完整性

| 区块 | 字段 | 类型 | 备注 |
|------|------|------|------|
| 基础信息 | name | text | 姓名（必填） |
| 基础信息 | gender | select | 男/女/其他 |
| 基础信息 | age | number | |
| 基础信息 | phone | text | |
| 基础信息 | wechat | text | |
| 基础信息 | email | email | |
| 基础信息 | source | select | 动态来源 |
| 业务信息 | product | text | |
| 业务信息 | budget | text | |
| 业务信息 | status | select | 4种状态 |
| 业务信息 | nextFollowDate | date | |
| 业务信息 | website | text | |
| 业务信息 | industryLevel1 | select-industry | 联动 |
| 业务信息 | industryLevel2 | select-industry2 | 联动 |
| 联系地址 | province | text | |
| 联系地址 | city | text | |
| 联系地址 | district | text | |
| 联系地址 | address | textarea | |
| 沟通备注 | remark | textarea | |

### ❌ 缺失字段（对比主客户表单）

| 缺失字段 | 主表单 | 池表单 | 影响 |
|---------|--------|--------|------|
| `company` | ✅ 有 | ❌ 无 | ⚠️ **严重**：池详情显示 company 但无法编辑 |
| `level` | ✅ 有 | ❌ 无 | 低（池设计无等级） |
| `tags` | ✅ 有 | ❌ 无 | 低 |
| `platforms` | ✅ 有 | ❌ 无 | 低 |
| `channels` | ✅ 有 | ❌ 无 | 低 |
| `amount` | ✅ 有 | ❌ 无 | 低 |
| `remarkHistory` 图片 | ✅ 支持 | ❌ 无粘贴/拖拽 | ⚠️ **中等**：截图功能缺失 |

**校验规则**: name 必填、phone 11位、email 格式、age 0-120 ✅

---

## 汇总：发现的问题与风险等级

| # | 问题 | 位置 | 严重度 |
|---|------|------|--------|
| 1 | **池表单无 company 字段，但池详情显示 company** | poolForm / poolDetail | ⚠️ **高** — 用户看到的"公司名称"无法编辑，且导出含此列 |
| 2 | **池详情跟进记录无图片支持** | openPoolCustomerDetail (行 ~5985) | ⚠️ 中 — 主详情和存量详情均有图片时间线 |
| 3 | **池删除无二次验证** | poolDelDetail | ⚠️ 中 — 主详情有 promptPassword |
| 4 | **池导出复用主客户格式** | poolExportOne → buildExcelData | ⚠️ 中 — 导出 26 列包含池无法编辑的字段 |
| 5 | **主详情编辑按钮的跨表匹配逻辑可疑** | editDetail 行 ~7037 | ⚠️ 低 — 用 id 交叉匹配存量子表，id 冲突概率极低 |
| 6 | **stat-popup 不包含池客户/存量客户** | showStatPopup | ✅ 设计决策（仅统计主客户） |
| 7 | **浮层面板不显示 email/gender/age/地址/行业** | showCustomerFloat | ✅ 设计精简（快速预览） |
| 8 | **池详情不显示 level/tags/platforms** | poolDetail | ✅ 设计决策（池无这些字段） |

### 建议修复优先级

1. **高优先级**: 池表单增加 `company` 字段（与池详情对齐）
2. **中优先级**: 池表单增加截图粘贴/拖拽功能；池删除增加二次验证；池导出改用池自有格式
3. **低优先级**: 评估主详情编辑按钮交叉匹配逻辑是否移除

---

## 审计结论

- **主客户详情/表单/导出**: ✅ 字段完整，关联正确，安全措施到位
- **存量客户详情/表单/导出**: ✅ 字段完整，通过 `channelData` 实现良好的模块隔离
- **统计浮层**: ✅ 数据来源正确（`Store.data.customers`），打开/关闭/恢复流程完整
- **客户浮层面板**: ✅ 字段精简合理，数据绑定正确，操作按钮逻辑正确
- **客户池详情/表单/导出**: ❌ 存在 company 字段缺失的显式问题，导出格式不匹配，图片和安全性弱于主模块
