# 2026-06-22 工作日志

## 存量客户管理 - 意向产品改为单选下拉（仅限存量三模块）

- **需求**：仅"存量客户管理"下的"品牌客户管理"、"服务费管理"、"企服产品管理"三个模块，点击联系人/公司名弹出客户详情编辑浮层时，"意向产品"栏从自由文本输入改为单选下拉，选项来自"产品标签管理"模块的标签数据。不修改其他模块。
- **改动范围**：`index.html` 中三处修改：
  1. `openStockCustomerForm` 字段定义（第 6673 行）：`{ key: 'product', label: '意向产品', type: 'text', max: 100 }` → `{ key: 'product', label: '意向产品', type: 'select-product' }`
  2. `openStockCustomerForm` 渲染分支（第 6746-6759 行）：新增 `select-product` 类型处理，从 `Store.data.productTags` 读取标签列表生成 `<select>` 下拉，兼容对象/字符串两种数据格式
  3. `openCustomerDetail` 编辑按钮（第 6961-6974 行）：检测客户是否属于存量模块（brandProducts/serviceFees/qiFuProducts/stockCustomers），是则调用 `openStockCustomerForm`，否则走原来的 `openCustomerForm`
- **未改动**：`openCustomerForm`（潜在客户/渠道模块编辑）、其他模块、系统框架均保持不变。
