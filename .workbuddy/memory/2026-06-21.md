# 2026-06-21

## 各频道金额漏斗功能优化

根据 `CRM-修正版-各频道金额漏斗方案.md` 文档，在现有系统框架内完成以下改动（不修改 Store/框架逻辑）：

1. **COLUMN_DEFS + renderColumnCells** — 新增 `amount` 列定义和渲染逻辑，频道表格可显示"预计金额(元)"
2. **频道编辑表单** — 业务信息区新增 `amount` 数字输入框，支持频道上下文读取/保存/重置
3. **频道 AI 摘要** — 新增金额统计：预估总额、跟进中金额、已成交金额
4. **BI 看板金额漏斗** — 新增 HTML section 和 `renderChannelFunnel` 函数，表格化展示各频道按阶段（未沟通/跟进中/已成交/流失）的客户数和金额汇总

遵循原则：不改 Store 架构，不改数据流，不改现有字段体系，纯做加法。
