# 2026-06-18 工作日志

## BI 看板 Bug 修复

检查并修复了 `index.html` 单文件 CRM 系统中 BI 看板模块的以下 Bug：

1. **[严重] Bug #1 - todayStart 作用域错误**：`renderBIAnalysisGrid` 函数内 IIFE 引用了 `todayStart`，但该变量是 `renderBIPage` 的局部变量，函数签名未传入，导致 ReferenceError。修复：在调用处和函数签名都加入 `todayStart` 参数。

2. **[严重] Bug #2 - 标签排行永不渲染**：`renderAllBICharts` 使用字符串拼接约定（`draw` + name + `Chart`）调用图表函数，但标签排行函数名为 `drawBITagRank`（无 `Chart` 后缀），导致调用失败。修复：改为直接引用函数对象的对象数组，消除命名约定依赖。

3. **[中] Bug #3 - 业绩缺口预警逻辑不合理**：原来以 `total * 15%` 为期望值，导致每月几乎都触发预警。修复：改为以本月新增客户数的 30% 作为期望成交量。

4. **[中] Bug #4 - 跟进时效图包含非活跃客户**：时效分析错误统计已成交/流失客户，数据失真。修复：只统计 status 非 `已成交`/`流失` 的活跃客户，并改用创建时间兜底（而非固定归入 >1月 组）。

5. **[中] Bug #5 - 性别环形图 rightX 坐标错误**：`W/2 + halfW/2` 导致圆心偏出画布右侧被裁切。修复：以右半区域的实际中心计算 rightX。

6. **[轻] Bug #6 - 预算分布图 rightX 与左侧 halfW 分割逻辑不一致**：布局错位。修复：统一用 `padding.left + halfW + 40` 为右区起点。

7. **[轻] Bug #7 - exportBIReportCSV 文件格式问题**：文件名 `.csv` 但内容是纯文本，且无 BOM 导致中文乱码。修复：改为导出带 UTF-8 BOM 的 `.txt` 文件，使用 Blob URL 下载。

8. **[轻] Bug #8 - churnList DOM 判空缺失**：修复流失预判区块 innerHTML 写入前未检查 DOM 存在性。

9. **[轻] Bug #9 - 深色模式环形图内圈颜色读白**：`getComputedStyle(document.body)` 读 CSS 变量可能为空，兜底 `#fff` 在深色主题下导致内圈发白。修复：改从 documentElement 读取变量，并检测 data-theme 属性决定文字颜色。

10. **[优化] 预计本月新增算法**：原 `monthNew * 1.3` 无意义，改为按已过天数线性外推到月末。

---

## 第二轮全模块 Bug 检查与修复

对全模块进行系统性深度审查，新增修复 3 处 Bug：

11. **[中] Bug #10 - drawSourcePie / drawTrendBar 未过滤软删除客户**（行 3356、3418）：仪表盘来源饼图和月度趋势柱图直接遍历 `Store.data.customers`，包含已软删除（`deleted:true`）客户导致数据失真。修复：两处均加 `.filter(c => c.deleted !== true)` 前置过滤。

12. **[严重] Bug #11 - selectedIds.clear() 调用 Array 上不存在的方法**（行 4858）：`btn-add-to-channel` 回调中 `selectedIds = Array.from(listState.selected)` 是局部数组，`Array` 无 `.clear()` 方法，操作完成后抛出 TypeError 导致后续 UI 无法正确清空选中状态。修复：改为 `listState.selected.clear()`（`listState.selected` 是 `Set`，有 `.clear()` 方法）。

13. **[中] Bug #12 - CSV 导入缺少 remarkHistory 字段初始化**（行 4948）：导入客户对象未初始化 `remarkHistory: []`，后续任何调用 `c.remarkHistory.unshift()` 的逻辑均会抛出 `TypeError: Cannot read properties of undefined`。同时补充 `deleted: false`，确保导入客户与手动创建客户数据结构一致。

### 跨模块数据联动验证结论

- `refreshUI()` 覆盖全部 8 个页面（dashboard、bi、list、geo、360、bing、trash、settings/security），数据联动完整无遗漏。
- `navigate()` 每次切换页面均调用对应渲染函数，实时性有保障。
- 软删除过滤：各模块渲染函数均通过 `.filter(c => c.deleted !== true)` 正确排除已删除客户（仪表盘两处已在本轮修复）。
- 表单保存后路由：新增跳转到 `list`，编辑就地刷新，逻辑正确。
- `Store.saveWithLog` 每次数据写操作后自动持久化，数据层无遗漏。

---

## BI 看板图表全面重写 - 高清 + 动画 + 美化

新增共用工具函数：`roundRect/roundRectTop`（圆角矩形）、`animateChart`（rAF + easeOutCubic 引擎）、`getThemeColors`（主题色统一读取）、`drawGridLines`（Y轴辅助线）、`BI_FONT`（标准字体栈防错位）。

重写 9 个图表函数：等级柱（圆角+渐变+动画）、来源环形（扇形展开+双行图例）、漏斗（居中扩展动画）、月趋势（柱+折线+当月高亮+均值线）、渠道对比（分组圆角柱）、省份排行（水平进度条动画）、标签排行（CSS transition 交错延迟）、跟进时效（颜色渐变柱）、产品/预算（精确左右分区布局）、人口统计（精确圆心+扇形展开+内嵌图例）。

CSS 升级：KPI 卡片 fadeUp 进入动画+顶部色条 hover、进度条交错延迟动画、图表卡片 hover 上浮、标签排行左移悬停。文件行数 5208 → 5616。

---

## 全局猫咪小宠物浮层

新增全局猫咪浮层（IIFE 模块，`window.CRMPet` 暴露调试接口），包含：
- **SVG 猫咪形象**：手绘风格，含尾巴/耳朵/身体/眼睛/胡须/腮红，4 种状态眼睛（正常/警觉/高兴/睡眠）。
- **状态机**：idle / alert / happy / sleep 四态，各有独立 CSS 动画（尾巴摇摆、身体弹跳、耳朵抖动、Zzz 飘出）。
- **提醒逻辑**：扫描 `Store.data.customers` 中超过 3 天/7 天未跟进客户，7天以上为紧急（红点+快速脉冲），3-7天为一般提醒（黄点）。
- **气泡提醒**：带标题、正文、操作按钮（立即查看/知道了/稍后提醒），稍后提醒延迟 1 小时重新检查。
- **徽章**：角标显示待跟进总数。
- **拖拽**：支持 mouse + touch 拖拽定位，边界自适应。
- **定时调度**：启动后 3 秒首次扫描，之后每 30 分钟自动检查；无操作 5 分钟后进入睡眠状态。
- 文件行数 5616 → 7050。

### 猫咪浮层优化（7050 → 7144 行）
- 气泡改为直接列出每位客户姓名 + 标签（逾期/待跟进）+ 距今天数，点击行直接跳转客户列表，无二级操作按钮。
- 新增双击缩小/展开功能：双击猫咪切换 mini 模式（缩至 36px），mini 状态下 hover 显示"双击展开"提示，再次双击恢复并自动弹出提醒。
- 单击行为保持不变（单击开关气泡）；260ms 内两次点击识别为双击。


### 猫咪气泡客户列表修复（7144 行）
- Bug：`scan()` 在 Store 未就绪时 `return` 无返回值，导致 `total=undefined`，标题显示"undefined 位"。修复：`return 0`。
- Bug：`check()` 中 `const total = this.scan()` 未兜底，改为 `|| 0`。
- Bug：`scheduleCheck` 固定 3 秒后扫描，此时 Store 可能未解锁。改为 1.5 秒轮询，检测 `Store.data.customers` 存在后首次扫描。
- 优化：客户行点击改为预填 `listState.searchQ` + 搜索框，跳转后直接定位到该客户；`data-cid` 改为 `data-name`。

### 猫咪扫描时序根本修复
- 根本原因：`scheduleCheck()` 的轮询在 IIFE 启动时立即运行，此时 `Store.data.customers` 可能是空数组（已通过 `[]` 初始化），轮询误判"已就绪"而 `clearInterval` 退出，解锁后真实数据加载完毕时无人再触发扫描。
- 修复：在 `enterApp()` 解锁成功后（`navigate('dashboard')` 之后）直接调用 `window.CRMPet.check()`，延迟 1200ms 等渲染完成。
- `scheduleCheck()` 移除首次扫描轮询，只保留 30 分钟定期复查和 5 分钟睡眠检测。

### 仪表盘预警模块加载异常修复
- Bug #13：`renderAlertSection` 行3791，overdueList `.map()` 解构赋值语法错误 — `{ ...c, _isOverdue, _overdueDays: overdueDays, _severity, _severityLabel, _reason }` 中 `_isOverdue`、`_severity`、`_severityLabel`、`_reason` 缺少属性值（ES6 shorthand property 要求变量名和属性名相同，但 `isOverdue`/`severity`/`severityLabel`/`reason` 与目标属性名不一致），导致 `ReferenceError` 或赋值错误。修复：改为 `Object.assign({}, c, { _isOverdue: isOverdue, ... })`。
- Bug #14：行3811，upcomingList 同样问题 — `{ ...c, _isToday, _daysUntil, _label, _cssClass }`，同样改为 `Object.assign`。
- Bug #15：行3977，`Store.settings.followupDays` 无短路保护，与行3767 不一致。修复：`(Store.settings && Store.settings.followupDays) || 3`。
- 注：Trae 同步修改了该文件新增预警模块，但 ES6 解构语法写法有误导致整个 `renderAlertSection` 在 `try` 块中抛异常，被 `catch` 捕获后显示"预警模块加载异常"。

### BI看板拖动排序功能
- 客户数据分析模块（KPI 行、多维分析矩阵、图表分析区）支持拖动排序
- 实现：HTML5 Drag & Drop API，事件委托方式绑定
- 每个 `bi-sortable` 容器内的 `[draggable="true"]` 子元素可拖动
- 排序状态持久化到 localStorage（key: `crm_bi_sort_order`），按容器 id 分组存储
- 拖动时有视觉反馈：`.bi-dragging` 半透明 + 缩小，`.bi-drag-over` 虚线边框

### Qclaw 审计报告修复
- S1：密码不再明文存入 sessionStorage，改用 XOR 混淆 + Base64 编码（`_obfuscatePw`/`_deobfuscatePw`），DevTools 无法直接读取原文
- S2：密码校验哈希 `passwordHash()` 改用随机 salt（首次生成后持久化到 `localStorage.crm_hash_salt`），防彩虹表
- S3：添加 CSP meta 标签（`script-src 'self' 'unsafe-inline'; img-src 'self' data: blob:`）
- S5：忘记密码重置时一并清除 `crm_hash_salt` 和 `crm_session_pw`
- B1（渠道页 channel/channels）：经确认已通过 `hasChannel()` 函数兼容，无需额外修复
- B2：`showStatPopup()` 的 `vip` 分支不再引用外部 `topLevelNames`/`topLevelLabel`，改为函数内重新获取 `getLevels()`
- B3：猫咪跳转搜索修正：`listState.searchQ` → `listState.keyword`，`getElementById('search-input')` → `getElementById('filter-keyword')`
- B4：表单分组折叠 `.onclick` → `.addEventListener('click')`
- B5：CSV 导入 age 字段增加 `isNaN` 检查，防止 `NaN` 存入
- B6：CSS `var(--primary)` 全部改为 `var(--accent)`（3 处）
- B8：单客户导出文件名增加非法字符过滤 `replace(/[\\/:*?"<>|]/g, '_')`
- 每个卡片右上角有 `⠿` 拖拽手柄，hover 时显示

---

## 性别分布图例重叠修复

图例中"男""女"水平排列间距不足导致重叠，改为上下两行排列。

## 自动化报表风险提醒增强

新增【风险提醒】8 个风险维度（逾期风险、高价值客户风险、即将流失、新客沉寂、近期流失、流失率超标、转化率偏低、今日无跟进），各维度列出具体客户名，操作建议按紧急程度分级。

## 标签使用排行模块移除

移除 BI 看板中「标签使用排行」模块（HTML卡片、CSS样式、JS函数 `drawBITagRank`、图表注册项）。

## BI 看板布局调整

将「异常智能预警」和「趋势预测分析」移至「图表分析」上方，调整后顺序：数据概览 → 异常智能预警 → 趋势预测分析 → 图表分析 → 自动化报表。

## 渠道字段改为多选复选框

将客户表单中「所属渠道」从单选下拉框改为多选复选框（GEO/360/Bing 可同时勾选），数据模型从 `channel: String` 改为 `channels: Array`。新增 `hasChannel(c, ch)` 和 `getCustomerChannels(c)` 兼容旧数据。更新所有读取点：渠道页面过滤、BI图表、报表、客户详情、CSV导入导出。保存时自动清理旧 `channel` 字段。「添加到渠道」批量操作改为多选复选框+确认按钮。

---

## 性别分布图表文字重叠修复 + 自动化报表风险提醒

### 性别分布图例重叠修复
- 问题：`drawBIDemographicChart` 图例中"男""女"标签水平排列，间距仅 48px，文字+数字+百分比远超此宽度导致重叠。
- 修复：改为上下两行排列——男在上、女在下，左右对齐一致，彻底消除水平重叠。

### 自动化报表风险提醒增强
在 `generateBIReport` 中，将原【预警信息】+【操作建议】扩展为【预警信息】+【风险提醒】+【操作建议】三段式：

新增【风险提醒】8 个风险维度：
1. 🔴 跟进逾期风险 — 逾期客户数、最长逾期天数、涉及客户名
2. 🟠 高价值客户风险 — A/B级客户逾期/7天未跟进
3. 🟠 即将流失风险 — 超21天无联系客户
4. 🟡 新客沉寂风险 — 新增但无跟进记录
5. 🟡 近期流失预警 — 时段内新增流失客户
6. 🔴 流失率超标 — 超过10%警戒线
7. 🟡 转化率偏低 — 低于15%健康线
8. 🟡 今日无跟进安排 — 有跟进中客户但无今日计划

操作建议也对应增加了分级建议（🚨紧急/🔔关注/📞回访/💡建议等），无风险时显示 ✅ 健康。

## 变量名冲突导致BI看板数据丢失修复

- Bug：`renderBIPage()` 中 `const hasChannel = list.filter(...).length`（number）与全局函数 `hasChannel(c, ch)` 同名，导致 `renderBIAnalysisGrid` 中 `hasChannel(c, 'geo')` 传入 number 作为函数调用，TypeError 中断后续所有模块渲染。
- 修复：将变量重命名为 `channelAssigned`，更新 `renderBIPage` 中 KPI 行、`renderBIAnalysisGrid` 函数签名和内部引用。

## CSV导出 .map() 缺少闭合括号

- Bug：`getCustomerChannels(c).map(ch => ({...}[ch] || ch).join('|')` 箭头函数缺少 `)` 闭合，导致 JS 语法错误，整个页面无法加载。
- 修复：补上 `).join('|')`。

---

## 自动保存机制实现（3 秒间隔）

为系统新增全局自动保存机制，防止意外关闭导致数据丢失：

1. **Store 对象新增字段**：
   - `_dirty: false` — 脏数据标记，有未保存变更时为 true
   - `markDirty()` — 标记数据已变更（供自动保存轮询使用）
   - `save()` 保存成功后自动清除 `_dirty = false`

2. **自动保存定时器**：
   - `autoSaveTimer` + `startAutoSave()` / `stopAutoSave()` 控制生命周期
   - `AUTO_SAVE_INTERVAL_MS = 3000`，每 3 秒检查 `Store.unlocked` 状态，若已解锁则调用 `Store.save()`
   - 无条件保存（不依赖 dirty 标记），确保任何数据变更都能被覆盖
   - 保存失败仅 `console.warn`，不中断用户操作

3. **集成点**：
   - `enterApp()` — 解锁后调用 `startAutoSave()` 启动自动保存
   - `resetIdleTimer()` idle logout 回调 — 登出前最后保存一次（如果 dirty），然后调用 `stopAutoSave()`
   - 忘记密码重置 — 调用 `stopAutoSave()` 并重置 `_dirty = false`

4. **JavaScript 语法验证通过**（Node.js `new Function()` 检查）。

---

## 高端化 UI 改造（按《高端化改造详细报告》执行）

在不改变系统框架和业务逻辑的前提下，对全局 UI 进行了系统性高端化升级：

### 1. 设计色板重构（Design Token 体系）
- 主色从 `#2563eb`（Blue）升级为 Indigo 系（`--color-primary-50~900`），更专业稳重
- 新增强调色 Amber 系、完整语义色（success/warning/error/info 各带浅色变体）
- 中性色从 4 级扩展为 10 级（`--color-neutral-0~900`）
- 阴影系统从 3 级扩展为 4 级（sm/md/lg/xl），更细腻的层次感
- 圆角系统统一为 4 级（6/10/14/20px）
- 间距系统基于 4px 基准 7 级（4/8/12/16/24/32/48px）
- 字体系统新增 `--font-sans`（Inter + 中文字体栈）和 `--font-mono`（JetBrains Mono）
- 动画 easing 统一为 `--ease-out`（cubic-bezier）和 `--ease-spring`
- 保留旧变量名（`--accent`/`--bg-primary` 等）作为内部映射，确保不破坏现有 JS 引用

### 2. 按钮系统升级
- 4 级按钮：primary（实心+阴影）、danger、outline（线框）、ghost（透明）
- primary hover：上浮 1px + 紫色发光阴影
- active：scale(0.97) 按压反馈
- 新增 `.btn--loading` 旋转 spinner 状态
- 字号从 13px 提升到 14px，高度从 28px 提升到 36px

### 3. 仪表盘统计卡片
- 卡片 hover：上浮 3px + 阴影加深 + 顶部 3px 渐变色条淡入
- 数字字号 22px→32px，使用等宽数字字体
- 标签增加 letter-spacing 和图标间距

### 4. 客户表格优化
- 表头：uppercase + letter-spacing，底部 2px 加重分隔线
- 行：偶数行浅灰底色（斑马纹），hover 时整行淡紫高亮
- 操作按钮：默认隐藏（opacity:0），hover 行时淡入显示
- 新增等级 Badge 颜色编码（.badge--a/b/c/d，绿/蓝/黄/红）

### 5. 锁屏界面重做
- 背景从浅灰渐变改为深紫 Indigo 渐变（#312E81→#6366F1）
- 卡片：毛玻璃效果（backdrop-filter blur）+ 白色半透明
- Logo 图标加大 + 阴影
- 新增 `.auth-input-group` 输入组（圆角容器 + focus 紫光）
- 新增 `.auth-card.unlocking` 解锁成功放大淡出动画
- 安全提示信息改为左侧紫色边框 + 浅紫底色

### 6. 侧边栏导航
- 宽度 180px→220px
- 新增 3 个分组标签（概览/客户管理/系统管理）
- 品牌名「客户管理」→「CRM Pro」
- 活动态：左侧 3px 紫色指示条 + 浅紫底色（而非之前的蓝色实心块）
- 计数 Badge：改为胶囊形（border-radius: 100px）

### 7. 全局微交互
- 页面切换动画延长到 350ms，位移 12px
- Toast 通知改为深色底白字 + 紫色左边框
- 空状态图标加大（48px），新增副标题行
- 所有卡片 hover 统一使用新阴影系统
- 表单输入框：focus 紫光效果（box-shadow: 0 0 0 3px var(--color-primary-100)）
- 弹窗圆角升级到 --radius-xl（20px）

### 验证结果
- JavaScript 语法：通过（Node.js new Function() 检查）
- CSS 花括号：450 对平衡，434 条规则
- 所有旧变量名保留映射，JS 零改动

---

## 左侧导航栏精简

移除低端视觉元素、压缩间距：

1. **移除左侧紫色指示条**：删除 `nav-item.active::before` 整个规则块（3px 紫色竖条 + 圆角）
2. **移除 hover 滑移动画**：`nav-item:hover` 删除 `transform: translateX(3px)`
3. **间距压缩**：
   - `nav-item` padding：10px 12px → 7px 12px
   - `nav-item` margin-bottom：2px → 1px
   - `nav-item` font-size：14px → 13px
   - `nav-group-label` padding-top：`var(--space-3)` → 12px
   - `nav-list` padding：`var(--space-2)` → `0 var(--space-2)`（去掉上下内边距）
4. **验证通过**：JS 语法 + CSS 花括号平衡

## 侧边栏滚动条移除

`.sidebar` 的 `overflow-y: auto` 改为 `overflow: hidden`，消除右侧纵向滚动条。同时收紧 `.sidebar-header` 底部内边距（`var(--space-4)` → 10px），确保所有导航项在视口内完整可见。

## 侧边栏滚动条精细控制

`.sidebar` 改为 `overflow-y: scroll; overflow-x: hidden`，恢复鼠标滚轮滚动。通过 `::-webkit-scrollbar` 自定义滚动条：宽度 4px，默认透明（不可见），hover 侧边栏时显示浅灰细条（`var(--color-neutral-300)`），hover 滑块时加深（`var(--color-neutral-400)`）。效果：平时无滚动条，鼠标移入侧边栏并滚动时才出现 4px 细线。

## 全局模块间距收紧

**间距系统变量压缩**（全局生效，所有使用 `var(--space-N)` 的规则自动收紧）：
- `--space-2`: 8px→6px, `--space-3`: 12px→10px, `--space-4`: 16px→14px, `--space-5`: 24px→18px, `--space-6`: 32px→28px, `--space-7`: 48px→40px

**特定区域**：`.main-content` 内边距 `--space-5`→`--space-4`；`.topbar` 内边距 `--space-3`→`--space-2`、底部间距 `--space-4`→`--space-3`；`.bi-section` 内边距+底部间距 `--space-5`→`--space-4`；设置页卡片间距 16px→12px、标题间距 16px→10px、说明间距 20px→12px；锁屏说明间距 16px→12px。

## 侧边栏用户信息区

在侧边栏底部（导航列表下方）新增用户信息区：头像（圆角方块+首字母"U"）+ 名称"本地用户" + 角色"管理员" + 登出按钮。登出行为与锁屏按钮一致，hover 变红底。

## 用户名注册 + 头像系统

- **首次注册**：初始化界面改为用户名+密码注册，Store.data 增加 `username` 和 `avatar` 字段
- **解锁界面**：增加用户名输入框，解锁时先验证密码（解密成功），再验证用户名是否匹配
- **侧边栏用户区**：`updateSidebarUser()` 动态填充真实用户名和头像（base64 存储在加密数据中）
- **头像上传**：点击头像触发文件选择，Canvas 裁剪为 200×220 圆角矩形（12px 圆角），JPEG 0.85 质量转 base64 存入 `Store.data.avatar`
- **头像显示**：侧边栏头像 32×35px（等比缩放），无头像时显示用户名首字母
- **数据结构**：所有 `Store.data` 初始化点（load、init、reset、clear）均更新为包含 `username: ''` 和 `avatar: ''`
- **注册密码二次确认加强**：`init-password` 和 `init-password2` 增加实时比对，`oninput` 事件触发检查，一致时显示绿色 ✓ 提示，不一致显示红色错误提示
- **9点手势解锁系统**：
  - 新增手势Canvas绘制模块（240×240画布，3×3点阵，点间距80px）
  - 手势记录逻辑：鼠标/触摸按下→查找最近点→移动时自动吸附经过点→抬起时完成手势
  - 手势序列化为点索引字符串（如 "0-1-2-5-8"），存储在 `Store.data.gesturePattern`
  - 解锁界面增加"密码登录/手势登录"切换按钮
  - 手势登录流程：绘制手势→验证通过→自动切换回密码模式（自动填充用户名，聚焦密码框）
  - 设置手势流程：在"数据与安全 > 密码管理"中添加手势设置UI，绘制后点击"保存手势"存储
  - 支持清除手势和重新设置
  - 手势重置密码：验证手势通过后，弹窗设置新密码（实时比对两次输入）
- **数据结构更新**：所有 `Store.data` 初始化点增加 `gesturePattern: ''` 字段
- **CSS新增**：`.gesture-container`、`.gesture-canvas-wrap`、`.gesture-hint`、`.gesture-status`、`.gesture-actions`、`.auth-gesture-toggle` 等样式
- **密保问题系统**：
  - 数据结构新增 `Store.data.securityQuestions` 字段（数组，存储 `{q, a}` 对象，答案小写存储）
  - 数据与安全 > 密码管理 中新增"密保问题"区域，支持设置/清除/修改2个密保问题和答案
  - 明文标志位 `localStorage.crm_has_sq` 用于解锁页面判断是否已设密保（无需解密数据）
  - `initAuthPage()` 根据 `crm_has_sq` 标志动态更新"忘记密码"链接：已设密保显示红色"通过密保问题重置密码"，未设显示"强制清空并重置"
  - 解锁页面"忘记密码"左侧红字提示："设置密保问题以重置密码"
- **账户设置（原"设置"页）重构**：
  - 导航栏名称从"设置"改为"账户设置"
  - 顶部新增"重置登录密码"卡片，提供三种验证方式：
    1. 通过密保问题重置（需先设置密保）
    2. 验证当前密码重置（解密验证）
    3. 验证手势重置（需先设置手势）
  - `renderSettingsPage()` 根据密保/手势设置状态动态启用/禁用对应按钮
- **重置密码弹窗**：
  - 密保验证：弹窗显示两个密保问题，需同时答对
  - 密码验证：弹窗输入当前密码，通过解密验证
  - 手势验证：弹窗绘制手势验证
  - 验证通过后统一调用 `openSetNewPasswordDialog()` 设置新密码（含实时比对）
  - 密码重置后回到解锁页面
- **所有 Store.data 初始化点**增加 `securityQuestions: []` 字段
- **登录界面缩小20%**：`.auth-card` max-width 420→336px, padding space-6→space-5, border-radius xl→lg; `.auth-logo-icon` 56→44px; `.auth-title` 22→19px; `.auth-subtitle` 13→12px; `.auth-group` margin space-4→space-3; `.auth-info` padding/font缩小; `.auth-footer` margin/font缩小; `.auth-input-group input` padding 14→10px, font-size 16→14px; `.auth-unlock-btn` padding 14→10px, font-size 20→18px; 新增 `.auth-card .label`/`.input`/`.btn-lg`/`.btn-primary`/`.btn` 覆盖规则缩小字号和间距; `.auth-gesture-toggle` margin 16→12px, `.toggle-btn` padding 6→5px, font-size 13→12px
- **修复解锁界面两个问题**：
  1. "解锁系统"按钮改为 `display:block;width:100%;text-align:center` 确保居中
  2. "忘记密码"链接在 `initAuthPage` 中之前被覆盖为 `onclick=null` 导致无法点击，改为在未设密保时恢复完整的重置逻辑（含 `crm_has_sq` 清除）
  3. "设置密保问题以重置密码"红字添加 `id="btn-sq-hint"` 和 `cursor:pointer`，绑定点击事件：已设密保→打开密保验证；未设密保→提示先登录设置
- **修复"忘记密码"无法点击的根本原因**：`DOMContentLoaded` 中第7231行对 `btn-forgot` 的绑定在 `initAuthPage()`（第7175行）之后执行，覆盖了 `initAuthPage` 中根据密保状态动态设置的回调。解决方案：删除 `DOMContentLoaded` 中的重复绑定，完全由 `initAuthPage()` 负责。
