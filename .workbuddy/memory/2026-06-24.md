# 2026-06-24 工作日志

## 系统审查修复（基于 SYSTEM_FULL_AUDIT_2026-06-24.md）

### P0 修复（5 个）

1. **Bug 1**: `_dashboardMode`/`_biMode` 用 `let` 声明 → 改为 `window._dashboardMode`/`window._biMode`，`refreshUI()` 中的 `window.*` 判断不再永为 false。

2. **Bug 2**: `renderStockDashboard()` 中调用 `renderStockAISummary()` 函数名与后定义的带参版本冲突 → 将 dashboard 版本重命名为 `renderStockDashboardAISummary()`，消除同名覆盖。

3. **Bug 3**: `navigate('dashboard')` 总启动 `startAIRefresh()` → 改为根据 `_dashboardMode` 判断：sales 模式启动 `startAIRefresh()`，stock 模式启动 `startStockAIRefresh()`。非 dashboard 页增加 `stopStockAIRefresh()`。

4. **Bug 4**: `showStatPopup()` 不处理 `stock-*` 类型 → switch-case 新增 `stock-total`/`stock-list`/`stock-brand`/`stock-service`/`stock-qifu` 5 个分支。

5. **Bug 5**: 存量表单零字段验证 → 保存前增加 name/phone/email 验证。

### P1 修复（5 个）

6. **Issue 6+10**: 客户池删除无 `promptPassword` + 所有池操作用 `Store.save()` 而非 `saveWithLog()` → 删除增加密码验证，编辑/新增/删除均改用 `saveWithLog()`。

7. **Issue 7**: 客户池跟进记录无图片支持 → 池详情 timeline 增加 `r.images` 渲染（`timeline-thumb` 缩略图 + `openImageViewer` 全屏查看）。

8. **Issue 8**: 存量表单 `Object.assign` 导致 channelData 数据冗余 → 在 assign 前删除 `channelManaged` 字段（level/status/amount/nextFollowDate/tagIds）。

9. **Issue 9**: 存量表单无 HTML 转义 → 保存前对字符串字段（除 remark）执行 `escapeHTML()`。

### 回滚方式

已在 git 上创建 backup commit `50eb310`，执行 `git checkout -- index.html` 即可回滚到本次修改前。
